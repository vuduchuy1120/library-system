@page
@model LibrarySystem.Pages.BorrowDetails.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>BorrowDetail</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="BorrowDetail.BorrowId" />
            <div class="form-group">
                <label asp-for="BorrowDetail.UserId" class="control-label"></label>
                <select asp-for="BorrowDetail.UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="BorrowDetail.UserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BorrowDetail.BookId" class="control-label"></label>
                <select asp-for="BorrowDetail.BookId" class="form-control" asp-items="ViewBag.BookId"></select>
                <span asp-validation-for="BorrowDetail.BookId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BorrowDetail.BorrowDate" class="control-label"></label>
                <input asp-for="BorrowDetail.BorrowDate" class="form-control" />
                <span asp-validation-for="BorrowDetail.BorrowDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BorrowDetail.EndDate" class="control-label"></label>
                <input asp-for="BorrowDetail.EndDate" class="form-control" />
                <span asp-validation-for="BorrowDetail.EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BorrowDetail.Status" class="control-label"></label>
                <input asp-for="BorrowDetail.Status" class="form-control" />
                <span asp-validation-for="BorrowDetail.Status" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
