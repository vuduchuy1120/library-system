@page
@model Library_System.Pages.Publishers.IndexModel

@{
	ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
	<a asp-page="Create">Create New</a>
</p>
<table class="table table-bordered table-striped">
	<thead>
		<tr>
			<th class="text-center">
				@Html.DisplayNameFor(model => model.Publisher[0].PublisherName)
			</th>
			<th class="text-center">
				@Html.DisplayNameFor(model => model.Publisher[0].Address)
			</th>
			<th class="text-center">
				@Html.DisplayNameFor(model => model.Publisher[0].DeleteAt)
			</th>
			<th class="text-center">
				Action
			</th>
		</tr>
	</thead>
	<tbody>
		@foreach (var item in Model.Publisher)
		{
			<tr>
				<td class="text-center">
					@Html.DisplayFor(modelItem => item.PublisherName)
				</td>
				<td class="text-center">
					@Html.DisplayFor(modelItem => item.Address)
				</td>
				<td class="text-center">
					@Html.DisplayFor(modelItem => item.DeleteAt)
				</td>
				<td class="text-center">
					<a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
					<a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
					<a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
				</td>
			</tr>
		}
	</tbody>
</table>
